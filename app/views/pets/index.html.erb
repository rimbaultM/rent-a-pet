<%= render 'shared/navbar' %>
    <!-- integration flash alerts devise -->
<%= render 'shared/flashes' %>

 <h1 class="index-title pt-5">Choisissez votre partenaire de sortie</h1>

<div class="indexcontent">
  <div class="cards">
    <% @pets.each do |pet| %>
      <%= link_to pet_path(pet), class: 'no-deco' do %>
        <div class="card-trip">
          <%= cl_image_tag(pet.photo.key) %>

          <div class="card-trip-infos">
            <h2 class="pt-3 pet_name"> <%= pet.name %></h2>
            <h2 class="card-trip-pricing pet_price"><strong> <%= pet.price_per_day %></strong>€/ jour</h2>
<!--             <h2 class="card-trip-pricing pet_price"><%= pet.user.username %></h2>
 -->            <%= cl_image_tag pet.user.photo.key, class: 'card-trip-user avatar-bordered' %>
          </div>
        </div>
      <% end %>
    <% end %>
    </div>
      <img id="map" src='https://raw.githubusercontent.com/lewagon/layouts-demo/gh-pages/images/mapbox.png'>
  </div>


<%= render 'shared/footer' %>
